package Tournament.Build;

public class Fight {
    private Fighter user1;
    private Fighter user2;
    private Fighter CPU;


    public void createFighter() {

    }

    public void fightTurn() {
        while (user1.getCurrentHealth() > 0 && (user2.getCurrentHealth() > 0 || CPU.getCurrentHealth() > 0)) {

        }
    }


}
